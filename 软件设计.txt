Go-DEX Build 20101117
1、轮询前判断所有节点的各值是否为0，若为0则显示离线。
2、软件启动时首先检查当前目录的配置文件default.xml是否存在，若不存在则创建，若存在则读取配置。
3、配置文件的路径默认为软件的启动目录，最好不修改配置文件的目录。
4、节点登记成功后，发送请求上传当前烟雾和气流的值。

Go-DEX Build 20101118
1、地图实现
地图生成一个配置文件
<?xml version="1.0" encoding="utf-8"?>
<Map path="">
   <zoneMap zoneID="1" imgPath="" />
   <zoneMap zoneID="2" imgPath="" />
</Map>

创建节点配置文件的时候自动创建 节点_Map的配置文件。
zoneMap的ID可以设置为1-100；
2、抽气泵转速 ： 低中高；


地图
实时曲线
中英文版
操作说明书
日志查询与报告

3、巡检返回 登记信息
4、报警上传气流值
5、全局静音 功能

1、四管单区 ，请求上传烟雾值正常，请求上传气流值 只上传通道4的值。

2、硬件复位，监控界面也复位。增加协议。

20110313bug: 
软件对节点复位的时候 如果有警报存在，会出现传输数据的丢失，若无警报存在则一切正常；
软件修改火警阈值后，再次修改到正常值无法复位；
阈值大小限制；

修改node显示面板 4管4区,面板增加 型号 显示,时间协议处理，地图鼠标滚轮缩放。

20110330发现的几个问题：
1、上电登记按ID排序；
2、设备类型决定通道显示样式的那个深灰色可以稍微浅一点；
3、单个节点显示区里面，最上方的留空和下方间隙一样；
4、登录窗口里面的级别用中文表示，窗口可以美化一点，软件监控界面看看可以安排背景图，不好搞就算了；
5、现在日志查询是按勾选关键字查找的吧，按“火警”关键字查找，会把与火警无关的火警门限等记录也算上了，关键字应该改为“火警警报”？另外当同时勾选“火警”和“监控节点-6”的话，现在好像是按“或”来统计的？应该是“与”？

 增加了用户管理功能。


修改门限值，先检测值是否改变，改变的值才发命令，没改的不用发送。

修改门限开启线程处理。

请求烟雾值，开启线程处理。

20111129 bug:

1, 不锁定时，火警级别上升再降低时，不报低级别告警；再上升时，不报新的告警（回到正常以后再上升可以报）。
2、隔离继电器不能动作，应该进入隔离状态就动作，不论是否告警。现在的隔离操作是不是把全部继电器的电给停了？这样的话，这个隔离继电器就肯定不能动作了。
3、不锁定时，气流故障会连续上报，1秒钟1次；
4、现在的默认火警门限值分别是2、6、10、13，改为2、6、16、30.
5、告警状态下学习时，学习完了之后仍然是告警前的状态；应该学习后面跟个复位。
6、按键板F2灯应该是指示设定有效状态，在设定有效状态应该亮起。现在是在设定有效状态逾时自动退出时，F1、F2会亮。
7、曲线状态数据上传，确定一下，节点自动上传或由软件定时索取？

8、软件设置火警门限值时，也改为以烟雾水平灯数来计数？和现在的计数方式是1:2关系，1个灯对应2个AD数值。可以实际传输、计算时仍用AD值，仅在设定界面显示时以烟雾水平灯数来计数，这样改动小？现在软件请求上传节点状态后显示当前状态值为空（静音、锁定等状态，只有分类项目没有具体值）；软件admin登陆默认密码要复杂一些；软件里面的“停运”处理以及其他的各处无用框应该填上东西或加上功能，或取消或变灰；软件里面的火警延时设定的起始值不要为0秒，应该是1秒，转速设置处应该显示10%-100%，不要显示0-9；型号的分类  添加600型或只要600型；软件界面增加“读取以往报警”，注明仅可单机操作；要尽快改为数据库版本，符合这次的客户的查询要求。软件基本定稿后做繁体和英文版本（有翻译）。

20111203 bug
监控界面的"曲线“按钮不好用，按下后会出错，先不要了。右键菜单里面的”曲线“可以进去，但曲线显示不对劲，烟雾阈值不对，烟雾曲线的时间刻度不对，气流的阈值显示不对。
 
隔离跳窗口，右上角的底色要蓝色，现在无色
 
然后抓紧时间尽快搞出英文版

20151224：
1、修改轮询
2、节点设置界面顶部 读取设备类型 ， 其他灰色，隐藏 流速和传感器，其他栏里修改为设定滤网寿命及寿命剩余百分比；
3、历史火警 通道不正确；
4、设备编号 如果是字母有问题，数字没问题；

5、厂家1：有所有权限，
代理2：比1级只少了设置设备序列号功能
管理员3：比2级少了“系统设置”功能和读取设备序列号功能
操作员4：只有处警和报表功能
对于各级权限的用户，无权限功能应该搞灰色？或者看不见？看你方便
权限这个东西可以最后搞，别的都搞定再说

20151226
1、又发现个奇怪情况，软件里面有几个虚节点和一个实际节点，未开过巡检时候，都没有离线。设定离线检测周期为1分钟，断开网络，开始巡检，虚节点1分钟报离线，实际节点却要2分钟后才报离线。两种节点的区别应该就是实际节点曾经上传过在线信息，但测试时候是先断开网络再开始巡检的，两种节点应该是一样地位，实际上软件里面还是给区别对待了吧
2、用户权限，有个漏洞，就是代理2可以看见和修改厂家1的密码，应该不让看或修改
3、“厂家”改为“administrator” 
4、最小化后 Height must be greater than 0px; system.windows.forms.dataVisualization.chartpicture
system.WIndows.FOrms.Control.OnSizeChanged(EventArgs e)
System.WIndows.Forms.Control.UpdateBounds(int32 x,int32 y,...);
5、操作员4：只有处警、查看曲线、查看全局地图和报表功能，不给全局地图的编辑功能吧
6、打开全局地图时，对于当前有火警的节点，节点图标按火警等级变色显示，或不分火警等级只高亮或闪烁，无火警节点常色。
7、用户级别里面，“厂家”还是改为“生产商”吧，“代理”改为“代理商”吧，好不
8、各级用户的默认密码列一下  
 

20160408
气流正常 0  
气流低 -1 Gold 
气流高 1 DarkOrange  
气流故障 2 Gold 

火警正常 0 1Pink 2LightCoral 3Red 4Purple 
烟雾故障 -1Gold
